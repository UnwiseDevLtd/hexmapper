{
  "name": "hexmapper",
  "version": "0.3.0",
  "private": true,
  "description": "Trace an image into a hex-grid map, in the browser.",
  "license": "AGPL-3.0-or-later",
  "homepage": "https://codeberg.org/UnwiseDev/hexmapper",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "serve": "node build.js && python3 -m http.server 8000"
  }
}
